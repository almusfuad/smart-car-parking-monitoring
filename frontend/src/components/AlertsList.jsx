// Alerts list component
// TODO: Implement alerts display with severity badges (CRITICAL, WARNING, INFO)

const AlertsList = ({ alerts }) => {
  return (
    <div>
      <h2>Active Alerts</h2>
      <p>TODO: Display alerts list</p>
    </div>
  );
};

export default AlertsList;
