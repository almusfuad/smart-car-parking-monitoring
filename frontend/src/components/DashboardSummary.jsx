// Dashboard summary component with metrics cards
// TODO: Implement metrics display (total events, occupancy, devices, alerts)

const DashboardSummary = ({ data, loading }) => {
  return (
    <div>
      <p>Dashboard Summary - TODO</p>
    </div>
  );
};

export default DashboardSummary;
